export function render() {
  const root = document.getElementById("app-root");
  root.innerHTML = `<section class="card"><div class="inner"><h2>Admin • Hàng chờ duyệt</h2><p class="muted">(Phase 6 sẽ hiển thị & duyệt requests)</p></div></section>`;
}
